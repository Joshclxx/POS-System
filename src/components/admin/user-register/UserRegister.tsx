import React from "react";

const UserRegister = () => {
  return (
    <div>
      <p>USER REGISTRATION</p>
    </div>
  );
};

export default UserRegister;

// First Name, Nick Name, Middle Name, Last Name, Suffix, Email, Role, Password, Re-type Password
// Clear and Register Button
// Table, shows registered POS Users
// Table Actions "Edit and Delete User"
